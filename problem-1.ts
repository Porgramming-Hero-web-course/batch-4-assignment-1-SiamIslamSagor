{
  /* -------------------------------------------- */

  function sumArray(numbers: number[]): number {
    return numbers.reduce((acc, currentEl) => (acc += currentEl), 0);
  }

  /* -------------------------------------------- */
}
